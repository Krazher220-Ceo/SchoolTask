{
  "buildCommand": "npx prisma generate && node scripts/fix-migrations.js && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --include=dev",
  "framework": "nextjs",
  "regions": ["iad1"]
}

