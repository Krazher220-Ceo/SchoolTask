{
  "buildCommand": "npx prisma generate && (npx prisma migrate resolve --applied 20251209055319_init 2>/dev/null || true) && npx prisma migrate deploy && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}

